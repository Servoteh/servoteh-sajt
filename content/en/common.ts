/**
 * Shared EN strings — single source of truth for copy reused across pages.
 * Mirror of content/sr/common.ts.
 */

/**
 * CTA invitation text — used on generic pages (home, defence, references).
 * Pages with a tailored call to action keep their own text.
 */
export const ctaInviteBody =
  "Send us the basic details of your process, production need or technical requirement, and our team will propose the next steps.";
